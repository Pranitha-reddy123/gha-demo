name: env
on: push
env:
   APP_ENV: "UAT"
jobs:
   envjob:
     env:
       APP_ENV: "DEV"   
     runs-on: ubuntu-latest
     steps:
      - name: print env
        run:  echo "Welcome to $APP_ENV"
  
               
               
            
 

