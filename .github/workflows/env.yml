name: env
on: push
jobs:
   envjob:
     env:
       APP_ENV: "DEV"   
     runs-on: ubuntu-latest
     steps:
      - name: print env
        run:  echo "Welcome to ${{vars.PASSWORD}}"
   
  
               
               
            
 

